- op: add
  path: /spec/acme/solvers
  value:
    - dns01:
        cloudDNS:
          # The ID of the GCP project
          project: omniful-dev-infra
      # selector:
      #   dnsNames:
      #     - gcpdev.omniful.com
      #     - "*.gcpdev.omniful.com"